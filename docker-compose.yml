services:
  # 1. Database Service
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password # Change this!
      POSTGRES_DB: football_ai
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  # 2. Backend Service
  backend:
    build: ./backend
    shm_size: '2gb'
    restart: always
    depends_on:
      - db
    environment:
      - DB_CONNECTION_STRING=postgresql://admin:password@db:5432/football_ai
      - ALLOW_CSV_FALLBACK=false
      # Add other env vars from your .env file here
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"

  # 3. Frontend Service
  frontend:
    build:
      context: ./Dashboard/predictor-frontend
      dockerfile: Dockerfile
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  postgres_data: